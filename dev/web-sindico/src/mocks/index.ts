// Central mock entrypoint for web-sindico. Disable by turning the mock flags off.
export {
  initializeDevMode,
  isDevAuthBypassEnabled,
  isDevMockApiEnabled,
  isFullMockMode,
  isLocalDev,
} from '@/dev';
